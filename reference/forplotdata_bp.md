# Raw data for continous outcomes

Simulated raw data from 10 outcomes in a long format. Includes columns
for the outcome values (value), the outcome variable (variable) as
factor, and the treatment arm (arm) as a factor.

## Usage

``` r
forplotdata_bp
```

## Format

A data frame with 3 columns: `value`, `variable` and `arm`.
